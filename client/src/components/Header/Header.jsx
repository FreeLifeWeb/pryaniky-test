import React from 'react';
import { NavBar } from '../Navigate/Navigate';

const Header = () => {
    return (
        <header>
            <NavBar />
        </header>
    );
};

export default Header;
